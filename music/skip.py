import discord
import ytdl
import os
import api
from ytdl import *
from ytdl import Api
from discord.ext import modules
from discord.ext import commands
from ytdl import ytdl.exports

@bot.command()
async def skip(ytld.skip : fetch.current_song, votes=None):
  if user = skips:
    await bot.message(f"{usersInVC} People Need to vote in-order to skip):
      else:
        if user = in = VC.(alone==:discord.Utils):
        self.song.skip()
        
        def skip(self.skip):
          self.fetch.song()
          self.skip.song = True()
     
     skip = ytdl.skip.song.url = True():
       vc.play.new.song():
         skipSong()
