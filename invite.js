const = require("discord.js")
const = require("typescript.js")
const = require("commands.js")

prefix == set("/")

const => embed.Message({
  new = message.Embed()
  if message.content === 'invite' {
     .setTitle("Invite Detro")
     .setDescription("[invite me](https://top.gg/bot-invite/detrobot) - Invite Now)
     .setFooter("Invite Me Now..")
     .setColor(RANDOM)
     };
     await message.send(embed)
     await user.dm() {
       dm.message.content(text="Message Sent Successfully").embed.(type=Title);
       .setMessageDmColor(RANDOM)
       };
       message.send();
});
