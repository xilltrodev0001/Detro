const = require("discord.js")
const = require("cogs.js")

const => new.message(if member === {joins.guild}) {
   content = [
     .setTitle = "Welcome to {guild.server.name} you are now the {memberCountOnJoined} Member Now",
     .setThumbnail = "images/welcome.jpeg",
     .setColor = "discord.Color_blue()"
     ]
     for channels = [
       topic = "WELCOME"
       // 'welcome', 'arrivals', 'joins', 'joins-leaves',
       if fetched == channel()
       send.message = true
       ]
   }
   await message.send()
